# cep-146-naa-2026-Lab09

## Project Overview
This is a course lab repository for CEP-146 (NAA 2026, Lab 09).
The `.gitignore` is configured for C/C++ projects (object files, executables, shared libraries, etc.).

## Structure
- No source code has been added yet — this is a fresh lab scaffold.
- Add your C/C++ source files to the project root or subdirectories as needed.

## Building
Use `gcc` or `g++` to compile C/C++ files, for example:
```bash
gcc -o main main.c
./main
```

## Environment
- Replit NixOS (stable-25_05 channel)
- Language: C/C++ (inferred from .gitignore)
